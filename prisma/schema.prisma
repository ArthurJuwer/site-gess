datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // vai estar no .env
}

generator client {
  provider = "prisma-client-js"
}

model Pessoa {
  id      Int    @id @default(autoincrement())
  nome    String
  idade   Int
  email   String @unique
}